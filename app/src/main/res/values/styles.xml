<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FullScreen" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="ItemGameTextView">
        <item name="android:maxLines">1</item>
        <item name="android:layout_marginLeft">@dimen/item_et_iv_margin</item>
        <item name="android:layout_marginBottom">@dimen/item_tv_margin_bottom</item>
        <item name="android:layout_marginRight">@dimen/margin_default</item>
    </style>
    <style name="ItemGameTextView.Primary">
        <item name="android:textSize">16sp</item>
        <item name="android:layout_marginTop">20dp</item>
    </style>
    <style name="ItemGameTextView.Secondary">
        <item name="android:textSize">14sp</item>
        <item name="android:layout_marginTop">44dp</item>
    </style>

</resources>
